name = "${WORKER_NAME}"
main = "src/worker.js"
compatibility_date = "2024-11-01"

[vars]
HF_USERNAME = "${HF_USERNAME}"
HF_SPACE    = "${HF_SPACE}"
REQUIRE_KEY = "${REQUIRE_KEY}"
WAKE_TIMEOUT_SECONDS = "120"
